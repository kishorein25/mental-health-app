apiVersion: v1
kind: Service
metadata:
  name: mental-health-service
spec:
  type: NodePort
  selector:
    app: mental-health-app
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30007
